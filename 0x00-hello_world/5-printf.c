#include <stdio.h>
/**
 * main- uses printf function to print a line
 *
 * Description:you are supposed to only use printf and put
 * Return:code should return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
